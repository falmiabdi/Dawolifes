<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Dev build: allow cleartext (HTTP) to the local dev API server.
      The API URL is injected at build time from the PC's LAN IP
      (see scripts/build-app.mjs). For production, switch to HTTPS
      and restrict this config to your real domain.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
